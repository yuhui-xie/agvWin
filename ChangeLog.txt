==================================================
2016-11-06
#note and todo
1.添加nav local_plan的停止策略。先距离判断到某一点，到达后不再判断距离，只转角度。
2.在1中，转角度，暂时先用loc的定位数据，最后改成ini中可以选用loc坐标还是odom原始坐标。
3.local_plan中对停止的判断写在get_distance_goal_helper函数里，不是用最后目标点判断，
  而是用一定距离外的路径点判断。现在逻辑上，应该影响不大，但最好还是改过来。
4.在到达目标点后，外部发goal，发一次会规划路径，但不会动，发第二次才会动。不知道为什么。
  可能是goalreach的设置问题。郭恒改的程序好像没这个问题。
5.程序拆分，模块化